# 中優先度改進總結

本文件整理目前專案已完成且已驗證的中優先度改進，內容以實際可運作的本地 API 版本為準。

## 已完成項目

### 1. 資料庫結構與索引

`backend/db/init.sql` 已建立以下資料表：

- `products`
- `app_settings`
- `orders`

並為常用查詢欄位建立索引：

- 商品名稱、價格、庫存、建立時間
- 設定主鍵
- 訂單號、顧客 Email、狀態、建立時間

ER 圖已同步補到 [README.md](README.md) 的資料庫設計章節，方便直接查看表與表之間的關係。

### 2. API 分頁與查詢

`GET /api/products` 已支援：

- `page`
- `limit`
- `sort`
- `order`

回傳格式包含 `data` 與 `pagination`，可讓前端直接渲染列表。

### 3. 記憶體快取

後端對下列 API 加上快取：

- `GET /api/products`
- `GET /api/settings`

在商品或設定變更後，相關快取會被清除，避免讀到舊資料。

### 4. Docker 化部署

目前可透過 Docker Compose 啟動：

- PostgreSQL
- Express 後端
- 前端頁面

這讓本機開發與環境重建更容易。

### 5. 驗證與登入

後端已加入：

- JWT 驗證
- 輸入驗證
- 請求限速
- HTTP 日誌

管理員登入可用預設帳密進行測試：

- `admin / admin`

### 6. 前端文件與操作流程

前端已整理成單頁操作流程，包含：

- 商品瀏覽
- 購物車
- 管理登入
- 商品 CRUD
- 設定儲存
- WhatsApp / PDF 輸出

## 目前可直接驗證的功能

- 商品列表可從 API 讀取
- 購物車加減與刪除可用
- 管理後台登入可用
- 商品新增與刪除可用
- 設定儲存可用
- PDF 收據可輸出英文版與流水號

## 備註

- 舊版文件中提到的 Supabase 主流程，已不再是目前實際執行路徑。
- 現在以本地 PostgreSQL + Express API 為主。
