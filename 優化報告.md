# PetFood Mart 優化報告

## 報告目的

本文件整理目前專案的實際狀態、已修復問題與剩餘注意事項。內容以現有可運作的本地 API 版本為主。

## 當前架構

- 前端：Vue 3 CDN + Tailwind CSS
- 後端：Node.js / Express
- 資料庫：PostgreSQL 15
- 認證：JWT
- PDF：jsPDF
- 通訊：WhatsApp 連結

資料流為：

UI -> Vue 3 -> Express API -> PostgreSQL

## ER 圖

目前資料庫結構與關聯說明請見 [README.md](README.md) 的資料庫設計章節，已補上 Mermaid ER 圖與三張表的用途說明。

## 已修復的主要問題

### 1. Vue 模板無法正常編譯

已改為使用 Vue full build，並將前端邏輯整理成可直接運作的 Options API，避免模板無法渲染。

### 2. API 跨來源問題

已將後端 CORS 調整為允許目前實際使用的前端來源，包含：

- `http://localhost:3000`
- `http://127.0.0.1:3000`
- `http://192.168.47.1:3000`

### 3. 資料庫未初始化

已在 PostgreSQL 內建立並驗證：

- `products`
- `app_settings`
- `orders`

並補上示範商品資料。

### 4. 商品與設定 API 500

原因是資料表尚未建立或資料庫連線主機設定不一致。現在已改為可用的本機 PostgreSQL 連線設定，並完成驗證。

### 5. 管理登入失敗

已將後端管理員登入修正為可用的測試帳密：

- `admin / admin`

### 6. PDF 輸出不穩定

已把 PDF 匯出整理成英文收據版，使用固定版面與流水號，並移除不穩定依賴，現在可以正常產生 PDF blob。

## 目前已驗證功能

- 商品列表載入正常
- 語言切換正常
- 購物車新增、加減、刪除正常
- 管理登入正常
- 商品新增、刪除正常
- 設定儲存正常
- WhatsApp 提示正常
- PDF 輸出正常

## 仍需注意

- README 舊版內容已更新為本地 API 架構，但 `js/` 目錄仍保留早期模組化參考檔案。
- Tailwind 目前仍使用 CDN，開發上方便，但正式上線前建議改成本地建置。
- `orders` 表已建立，但目前前端主要是產生 WhatsApp / PDF，不是完整的訂單提交流程。

## 結論

目前專案已能穩定啟動與操作，資料庫、後端 API、前端互動與 PDF 輸出都已能正常運作。後續若要進一步上線，建議優先處理：

- 前端建置流程正式化
- 訂單真正寫入 `orders`
- 管理員帳密改為正式資料來源
